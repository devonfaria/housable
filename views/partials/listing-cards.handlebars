<section class="container">
  <a href="/listings/{{id}}">
    <p>{{title}}</p>
  </a>
  <header>{{format_date createdAt}}</header>
    {{!-- <img src="/images/{{paintings.[0].filename}}" alt="{{paintings.[0].description}}"> --}}
</section>
